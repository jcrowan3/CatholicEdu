# AI Runtime Provenance

This legacy document is retained so existing links do not break. The canonical curriculum provenance record is the [AI Content Provenance Audit](ai-content-provenance-audit.md).

As of 2026-08-22, the application does not call an AI model at runtime. Re-run the canonical audit when adding an AI SDK, model gateway, prompt, generation feature, or a new surface that transforms bundled curriculum.
